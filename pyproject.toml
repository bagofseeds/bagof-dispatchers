[project]
name = "bagof-dispatchers"
authors = [
    {name = "Yael Balbastre", email = "yael.balbastre@gmail.com"},
]
maintainers = [
    {name = "Yael Balbastre", email = "yael.balbastre@gmail.com"},
]
description = "Hint-informed multiple dispatch for Python"
readme = "README.md"
license = {text = "MIT"}
keywords = ["bagof", "dispatch", "multiple-dispatch", "typing"]
classifiers = [
    "Development Status :: 1 - Planning",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3 :: Only",
    "Typing :: Typed",
]
dynamic = ["version"]
requires-python = ">=3.8"
dependencies = [
    "typing_extensions>=4.13",
]

[project.urls]
Homepage = "https://github.com/bagofseeds/bagof-dispatchers"
Documentation = "https://bagofseeds.github.io/bagof-dispatchers/"
Issues = "https://github.com/bagofseeds/bagof-dispatchers/issues"
Repository = "https://github.com/bagofseeds/bagof-dispatchers"

[project.optional-dependencies]
test = [
    "pytest>=7",
]

[build-system]
requires = [
    "setuptools>=59.0",
    "wheel>=0.45.1",
    "versioningit>=1.0",
]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.package-data]
"bagof.dispatchers" = ["py.typed"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["bagof*"]
namespaces = true

[tool.versioningit]
default-version = "0+unknown"

[tool.versioningit.vcs]
default-tag = "0.1"

[tool.versioningit.format]
distance = "{base_version}+{distance}.{vcs}{rev}"
dirty = "{base_version}+{distance}.{vcs}{rev}.dirty"
distance-dirty = "{base_version}+{distance}.{vcs}{rev}.dirty"

[tool.versioningit.write]
file = "src/bagof/dispatchers/_version.py"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
source = ["bagof.dispatchers"]

[tool.coverage.report]
exclude_also = [
    # Type-checking-only blocks never run at runtime.
    "if tx.TYPE_CHECKING:",
    "if tx.TYPE_CHECKING or",
]

[tool.ruff]
line-length = 79
target-version = "py38"

[tool.ruff.lint]
select = ["ANN", "B", "E", "F", "I", "UP", "W"]
ignore = [
    "ANN002",   # Do not type *args
    "ANN003",   # Do not type *kwargs
    "ANN401",   # Allow typing with Any
    # pyupgrade rules whose fixes break Python 3.8 (the supported floor)
    "UP006",    # list[...] annotations need 3.9 at runtime
    "UP007",    # X | Y unions need 3.10
    "UP035",    # deprecated typing import (typing_extensions is intentional)
    "UP045",    # X | None optionals need 3.10
]

[tool.codespell]
check-filenames = true
skip = ".git,*.egg-info,build,dist,*.pdf,*.svg"
# Domain terms, not typos: CLOS (Common Lisp Object System) and multipledispatch's
# `supercedes` function name (spelled that way upstream).
ignore-words-list = "clos,supercedes"
